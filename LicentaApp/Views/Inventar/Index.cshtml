@model IEnumerable<LicentaApp.Inventar>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CantitateDisponibila)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Magazine.Denumire)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Produse.Cod)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CantitateDisponibila)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Magazine.Denumire)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Produse.Cod)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
