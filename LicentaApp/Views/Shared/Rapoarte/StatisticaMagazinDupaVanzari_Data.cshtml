@model System.Data.Entity.Core.Objects.ObjectResult<GetStatisticaMagazineDupaVanzari_Result>

<div class="row">
    <table class="highlight">
        <thead>
            <tr>
                <th>Magazin</th>
                <th>Oras</th>
                <th>Vanzari totale</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in this.Model)
            {
                <tr>
                    <td>@item.Denumire</td>
                    <td>@item.Oras</td>
                    <td>@item.TotalVanzari</td>
                </tr>
            }
        </tbody>
    </table>
</div>